'use client';

// Import SCSS files in a client component to avoid server-side issues
import '../styles/design-system.scss';
import '../styles/liquid-glass.scss';

export default function GlobalStyles() {
  return null;
}